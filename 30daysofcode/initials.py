print('. '.join(list(map(lambda c: c[0].upper(), input('ENter your full name: >>>  ').split()))))

'''
Implementing in a function
def initials(full_name):
    return '. '.join(list(map(lambda c: c[0].upper(), full_name.split())))
'''